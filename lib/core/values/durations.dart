abstract class Durations {
  static const splashAnimation = Duration(seconds: 2);
  static const transition = Duration(milliseconds: 300);
  static const transitionToNavigate = Duration(seconds: 2);
  static const transitionToShowTuturial = Duration(milliseconds: 500);
}
